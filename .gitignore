# Terraform state (contains secrets!)
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*

# Terraform providers/plugins (too large for GitHub)
.terraform/
.terraform.lock.hcl

# Secret variable files (NEVER commit these)
*.secrets.tfvars
*.auto.tfvars
*.secrets.tf

# Environment-specific overrides
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp